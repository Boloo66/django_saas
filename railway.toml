[build]
builder = "DOCKERFILE"
dockerfilePath = "./Dockerfile"
watchPatterns = [
    "requirements.txt",
    "src/*",
    "railway.toml",
    "Dockerfile",
]

# [build]
# builder = "nixpacks
# [deploy]
# startCommand = "gunicorn -w 3 -b "[::]:8000" wsgi:application"